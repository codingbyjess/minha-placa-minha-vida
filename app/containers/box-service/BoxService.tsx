type BoxServiceProps = {
    icon: string;
};

const BoxService = () => {
    return (
        <div className="relative mt-5 w-70 h-60 border-2 border-(--second) rounded-xl flex items-center justify-center">
            
        </div>
    );
};

export default BoxService;
